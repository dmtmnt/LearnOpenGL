
1. Required Third-party libraries dependencies:
brew install glew
brew install glfw

2. Update paths in<repo_dir>CMakeLists.txt to the version of glew/glfw previously installed.

3. Generate Xcode Project:
 Create a folder <repo_dir>/build_xcode 
 'cd build_xcode'
 'cmake -G Xcode ..

